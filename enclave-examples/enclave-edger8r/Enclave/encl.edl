enclave {
	
	trusted {
        public void ecall_pointer_string([in, string] char *str);
    };
	
	untrusted {
        /* define OCALLs here. */
        void ocall_print([in,string] const char *str);
	};
};
